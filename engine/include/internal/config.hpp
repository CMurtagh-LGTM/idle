#pragma once

#include <cstddef>

// TODO have Arena dynamically grow rather than have a set size
constexpr size_t SPRITE_COUNT = 256;
constexpr size_t TEXT_COUNT = 256;
constexpr size_t SHAPE_COUNT = 256;
constexpr size_t TRANSFORM_COUNT = 256;
constexpr size_t CLICK_BOX_COUNT = 256;
constexpr size_t TIMER_COUNT = 256;
